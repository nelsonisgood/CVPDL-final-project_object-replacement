# CVPDL-final-project_object-replacement

An end-to-end image editing framework enabling object replacement using natural language prompts.

Usage

1. Make sure you have the required Python environment and dependencies installed.
2. Run the following command to start the replacement process:

```
python merge_gem_flo_inp.py
```

That's it — running the above command will execute the main script for object replacement using natural language prompts.
